spring:
  profiles:
    include: localstack
aws:
  provider: testcontainers-localstack

server:
  shutdown: immediate


management.endpoint:
  health.cache.time-to-live: 0
  info.cache.time-to-live: 0
